node_modules/
dist/
.turbo/
*.tsbuildinfo

# env / secrets — никогда не коммитить
.env
.env.*
!.env.example
*.pem
*.key

# состояние релиза — живёт на хосте (/opt/corelink), не в репозитории
release.env
release.prev.env
deploy.log
backups/

# go
apps/node-agent/bin/
apps/node-agent/dist/

# os
.DS_Store
