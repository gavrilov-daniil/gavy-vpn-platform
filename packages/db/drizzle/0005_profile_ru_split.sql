-- Флаг «выводить РФ-трафик мимо туннеля» на профиль.
-- Раньше правило «РФ-домены → freedom» добавлялось в каждый профиль безусловно,
-- и профили обратного сценария («Россия», «Белые списки», где РФ-ресурсы нужны ЧЕРЕЗ туннель)
-- гнали свой трафик напрямую — то есть просто не работали.
-- default true = текущее поведение, существующие профили не меняются.
ALTER TABLE "profile" ADD COLUMN IF NOT EXISTS "ru_split" boolean NOT NULL DEFAULT true;
